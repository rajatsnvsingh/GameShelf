/** Re-exports normalized metadata-provider contracts for runtime modules. */
export type {
  ArtworkReference,
  GameDetails,
  MetadataProvider,
  ProviderBinding,
  ProviderEntry,
  SearchCandidate,
} from './provider.types';
